import styled from 'styled-components';
import { FlexCentralized } from '@styles/ui/flex';

export const Container = styled(FlexCentralized)`
  position: fixed;
  width: 100vw;
  height: 100vh;
  left: 0;
  top: 0;
  background: rgba(255, 255, 255, 0.75);
`;
